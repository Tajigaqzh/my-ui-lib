# My UI Component Library

一个基于 Vue 3 的现代化 UI 组件库，采用 monorepo 架构。

## ✨ 特性

- 🎨 基于 Vue 3 + TypeScript
- 📦 Monorepo 架构，使用 pnpm workspace
- 🔨 统一的构建配置，一次构建所有包
- 🧪 完整的测试覆盖（Vitest）
- 📝 完善的类型定义
- 🎯 支持 ESM 和 CJS 两种格式
- 🚀 开箱即用的 Playground 演示

## 📦 包结构

```
packages/
├── core/       # 核心组件（Button, Card 等）
├── utils/      # 工具函数
├── theme/      # 主题样式
└── icons/      # 图标
```

## 🚀 快速开始

### 安装依赖

```bash
pnpm install
```

### 构建组件库

```bash
pnpm run build
```

### 运行 Playground

```bash
# 先构建组件库
pnpm run build

# 启动开发服务器
pnpm run dev:playground
```

访问 http://localhost:5173 查看组件演示

### 运行测试

```bash
# 运行所有测试
pnpm test --run

# Watch 模式
pnpm test

# 测试 UI
pnpm test:ui

# 生成覆盖率报告
pnpm test:coverage
```

## 📖 组件列表

### Button 按钮

基础按钮组件，支持多种类型和尺寸。

### Card 卡片

卡片容器组件，支持标题、阴影效果和自定义样式。

## 🛠️ 开发指南

### 添加新组件

1. 在 `packages/core/<component-name>/` 创建组件文件夹
2. 创建组件文件和入口文件
3. 在 `packages/core/index.ts` 中导出
4. 运行 `pnpm run build` 自动构建

### 项目结构

```
my-ui-lib/
├── .kiro/                 # AI 协作文档（需求管理）
├── packages/              # 源码包
├── playground/            # 组件演示
├── test/                  # 测试文件
├── dist/                  # 构建产物
├── vite.config.ts         # 统一构建配置
└── vitest.config.ts       # 测试配置
```

## 📚 文档

- [需求管理文档](.kiro/requirements.md) - 查看和提出新需求
- [构建优化说明](.kiro/summaries/build-optimization.md) - 构建配置详解
- [项目设置总结](.kiro/summaries/setup-complete.md) - 项目设置完成情况

## 🤝 与 AI 协作

本项目使用 `.kiro/` 文件夹管理 AI 协作文档：

- **提出需求**：编辑 `.kiro/requirements.md`
- **查看历史**：查看 `.kiro/summaries/` 中的总结文档
- **了解更多**：阅读 `.kiro/README.md`

## 📄 License

MIT
