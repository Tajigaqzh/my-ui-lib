# My UI Playground

这是一个用于展示和测试 My UI 组件库的演示项目。

## 使用方法

1. 首先在根目录构建组件库：

```bash
pnpm install
pnpm run build
```

2. 启动 playground 开发服务器：

```bash
pnpm run dev:playground
```

3. 在浏览器中打开 http://localhost:5173 查看组件示例

## 添加新组件示例

在 `src/App.vue` 中添加新的组件示例即可。
